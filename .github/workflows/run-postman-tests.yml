name: Run Postman Collection

on:
  push:
    branches: ["main"]
  schedule:
    - cron: '0 0 * * *'  # Запуск щодня опівночі (UTC)
  workflow_dispatch:

env:
  COLLECTION_FILE: REST.postman_collection.json
  RESULTS_DIR: testResults
  REPORT_FILE: htmlreport.html

jobs:
  run-api-tests:
    name: Execute API Tests
    runs-on: ubuntu-latest

    steps:
      # 🧾 Клонуємо репозиторій
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ⚙️ Встановлюємо Node.js
      - name: Setup Node.js environment
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      # 📦 Встановлюємо залежності
      - name: Install Newman and reporters
        run: npm install --legacy-peer-deps

      # 📁 Створюємо директорію для звітів
      - name: Create testResults directory
        run: mkdir -p ${{ env.RESULTS_DIR }}

      # 🚀 Запускаємо Postman колекцію та генеруємо звіти
      - name: Run Postman collection and generate reports
        run: |
          set -e  # Завершить job з помилкою, якщо newman завершиться з error code

          npx newman run ${{ env.COLLECTION_FILE }} \
            -r htmlextra,json,cli \
            --reporter-htmlextra-export ${{ env.RESULTS_DIR }}/${{ env.REPORT_FILE }} \
            --reporter-json-export ${{ env.RESULTS_DIR }}/report.json \
            > ${{ env.RESULTS_DIR }}/output.txt

      # 🔍 Виводимо логи Newman (для зручності)
      - name: Print Newman output
        if: always()
        run: cat ${{ env.RESULTS_DIR }}/output.txt

      # ☁️ Завантажуємо артефакти (html + json)
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: API_test_reports
          path: ${{ env.RESULTS_DIR }}

      # 📤 Переміщуємо HTML-звіт у директорію gh-pages
      - name: Move HTML report to deployment directory
        run: |
          if [ -f ${{ env.RESULTS_DIR }}/${{ env.REPORT_FILE }} ]; then
            mkdir -p gh-pages/test-report
            mv ${{ env.RESULTS_DIR }}/${{ env.REPORT_FILE }} gh-pages/test-report/index.html
          else
            echo "❌ Report not found. Skipping move step."
          fi

      # ⚙️ Налаштування git
      - name: Configure Git for push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      # 🚀 Деплой на GitHub Pages
      - name: Deploy test report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        if: always()
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: gh-pages
          enable_jekyll: false

# https://katerynaa13.github.io/postman/test-report/
