name: Run postman collection

on:
  push:
    branches: ["main"]
  schedule:
    - cron: '0 0 * * *'  # Запуск раз в сутки в полночь UTC
  workflow_dispatch:

jobs:
  run-api-tests:
    name: Execute API Tests
    runs-on: ubuntu-latest

    steps:
      # 🧾 Клонируем репозиторий
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Загружает полную историю

      # ⚙️ Устанавливаем Node.js
      - name: Setup Node.js environment
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      # 📦 Устанавливаем зависимости (с флагом --legacy-peer-deps для избежания ошибок)
      - name: Install Newman and reporters
        run: npm install --legacy-peer-deps

      # 📁 Создаём директорию testResults
      - name: Create testResults directory
        run: mkdir -p testResults

      # 🚀 Запускаем Postman коллекцию и генерируем html-отчёт
      - name: Execute REST collection
        run: |
          newman run REST.postman_collection.json -r htmlextra,cli --reporter-htmlextra-export testResults/htmlreport.html

      # 🔍 Проверяем, что файл отчета создан
      - name: Check generated files
        run: ls -lah testResults || true

      # ☁️ Загружаем артефакты (отчёт)
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: API_test_reports
          path: testResults

      # 🏷️ Проверяем, есть ли ветка gh-pages
      - name: Checkout gh-pages branch
        uses: actions/checkout@v4
        if: always()
        continue-on-error: true
        with:
          ref: gh-pages
          path: gh-pages
          persist-credentials: false

      # 📤 Перемещаем html-отчет в директорию для деплоя
      - name: Move test report to deployment directory
        run: |
          if [ -f testResults/htmlreport.html ]; then
            mkdir -p gh-pages/test-report
            mv testResults/htmlreport.html gh-pages/test-report/index.html
          else
            echo "❌ Report not found. Skipping move step."
          fi

      # ⚙️ Конфигурация git перед пушем
      - name: Configure Git for push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      # 📦 Публикуем на GitHub Pages
      - name: Deploy test report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        if: always()
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: gh-pages
