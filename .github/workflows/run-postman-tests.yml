name: Run postman collection

on:
  push:
    branches: ["main"]
  schedule:
    - cron: '0 0 * * *'  # Запуск разів на добу опівночі UTC
  workflow_dispatch:

jobs:
  run-api-tests:
    name: Execute API Tests
    runs-on: ubuntu-latest

    steps:
      # 🧾 Клонуємо репозиторій
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Завантажує повну історію

      # ⚙️ Встановлюємо Node.js
      - name: Setup Node.js environment
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      # 📦 Встановлюємо залежності (з прапором --legacy-peer-deps для уникнення помилок)
      - name: Install Newman and reporters
        run: npm install --legacy-peer-deps

      # 📁 Створюємо директорію testResults
      - name: Create testResults directory
        run: mkdir -p testResults

      # 🚀 Запускаємо Postman колекцію та генеруємо html-звіт
      - name: Execute REST collection
        run: |
          npx newman run REST.postman_collection.json \
            -r htmlextra,cli \
            --reporter-htmlextra-export testResults/htmlreport.html \
            > testResults/output.txt

      # 🔍 Перевіряємо, що файл звіту створено
      - name: Check generated files
        run: ls -lah testResults || true

      # ☁️ Завантажуємо артефакти (звіт)
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: API_test_reports
          path: testResults

      # 📤 Переміщуємо html-звіт у директорію для деплою
      - name: Move test report to deployment directory
        run: |
          if [ -f testResults/htmlreport.html ]; then
            mkdir -p gh-pages/test-report
            mv testResults/htmlreport.html gh-pages/test-report/index.html
          else
            echo "❌ Report not found. Skipping move step."
          fi

      # ⚙️ Конфігурація git перед гарматою
      - name: Configure Git for push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      # 📦 Публікуємо на GitHub Pages
      - name: Deploy test report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        if: always()
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: gh-pages
          enable_jekyll: false

#https://katerynaa13.github.io/postman/test-report/
